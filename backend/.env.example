# Server Config
PORT=5000
FRONTEND_URL=http://localhost:5173

# Email Configurations
RECEIVER_EMAIL=tasoriajatin@gmail.com

# SMTP Settings
# Leave these BLANK during local development to simulate email dispatches via console logs.
# To send real emails, enter your SMTP provider credentials (e.g. Gmail App Password, SendGrid, Mailgun).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
